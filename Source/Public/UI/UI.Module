file(GLOB_RECURSE UI_HEADERS ${CMAKE_CURRENT_LIST_DIR}/*.h*)
file(GLOB_RECURSE UI_SOURCES ${CMAKE_CURRENT_LIST_DIR}/*.c*)

set(ENGINE_HEADERS ${ENGINE_HEADERS} ${UI_HEADERS})
set(ENGINE_SOURCES ${ENGINE_SOURCES} ${UI_SOURCES})

message(STATUS "Included UI module.")
